// import Vue from 'vue';
// import VueRouter from 'vue-router';
// Vue.use(VueRouter)
// import { constantRoutes, allAsyncRoutes } from "@/router";

// const router2 = new VueRouter({
//   mode: 'history', // 如果需要使用 history 模式，请启用该选项
//   routes: [...constantRoutes, ...allAsyncRoutes],
//   scrollBehavior() {
//     return { x: 0, y: 0 }; // 由于 Vue Router 3 中 scrollBehavior 选项的格式已改变，需要将 top 改为 y，left 改为 x
//   },
// });


// // 导出路由
// export default router2;